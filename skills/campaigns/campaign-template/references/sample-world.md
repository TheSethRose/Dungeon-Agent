# Sample World — copy and rewrite

The DM authors this per the `build-world` skill (grilling the player one
question at a time). Structure reference below. Replace every placeholder.

## Premise
<one paragraph — the hook and the stakes.>

## Starting gear & abilities
- Gear: <items from the schema whitelist>
- Abilities: <abilities from the schema whitelist>
- <anything else the character begins with>

## Locations (flows start → final)
1. **start_location** — <name + short description + exits>. Matches schema `start_location`.
2. **next** — <desc + exits>.
3. **final** — <the win-condition chamber>.

## Monsters
- <name> (hp X, atk Y, def Z) — <flavor>. XP <n>.

## Loot
- <item from whitelist> (<effect>)

## XP & leveling
XP to next level = <formula>. On level up: <what changes> (applied by the DM).

## Win condition
<repeat of schema win_condition>
